[versions]
kotlin-jvm = "2.3.10"
loom = "1.15-SNAPSHOT"

[plugins]
kotlin-jvm = { id = "org.jetbrains.kotlin.jvm", version.ref = "kotlin-jvm" }
loom-remap = { id = "net.fabricmc.fabric-loom-remap", version.ref = "loom" }
legacy-looming = { id = "legacy-looming", version.ref = "loom" }
